// MapLibre GL ładowany z CDN (index.html) → dostępny jako window.maplibregl
// eslint-disable-next-line @typescript-eslint/no-explicit-any
declare global { interface Window { maplibregl: any } }
export {};
